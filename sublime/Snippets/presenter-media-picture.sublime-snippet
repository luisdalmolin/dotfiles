<snippet>
	<content><![CDATA[
public function picture(\$w = 105, \$h = 75, \$fit = 'resize')
{
    if (\$media = \$this->model->medias->first()) {
        return \$media->present->picture(\$w, \$h, \$fit);
    }
}
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>presenter-media-picture</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.php</scope>
</snippet>