<snippet>
	<content><![CDATA[
public function upload${1:Cover}(\$${2:cover})
{
    if (\$${2:cover}) {
        \$this->uploadSingleMedia(\$${2:cover}, '${2:cover}');
        \$this->save();
    }
}
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>eloquent-upload-single-media</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.php</scope>
</snippet>